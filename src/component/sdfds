import React,{useState, useRef} from 'react';
import Button from '@material-ui/core/Button';
import Dialog from '@material-ui/core/Dialog';
import DialogActions from '@material-ui/core/DialogActions';
import DialogContent from '@material-ui/core/DialogContent';
import DialogContentText from '@material-ui/core/DialogContentText';
import DialogTitle from '@material-ui/core/DialogTitle';
import TextField from '@material-ui/core/TextField';
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
    root: {
      '& > *': {
        margin: theme.spacing(1),
        width: '25ch',
      },
    },
  }));

const AddIncome = (props) => {
    const classes = useStyles();
    const [expenseDetails, setExpenseDetails] = useState({
        amount:0,
        details:''
    });
    const amountRef = useRef();
    const detailsRef = useRef();

      

const incomeFormHandler = (event) => {
    event.preventDefault();
    console.log(amountRef.current.value);
    console.log(detailsRef.current.value);
   //  props.addExpense('income', expenseDetails);
   //  setExpenseDetails({
   //      amount:0,
   //      details:''
   //  });
}

  return (
    <div>
      <Dialog
        open={props.modalStatus}
        onClose={props.handleModalStatus.bind(this,'income')}
        aria-labelledby="alert-dialog-title"
        aria-describedby="alert-dialog-description"
      >
        <DialogTitle id="alert-dialog-title">{"Add Income"}</DialogTitle>
        <form className={classes.root} autoComplete="off" onSubmit={incomeFormHandler}>
        <DialogContent>
          <DialogContentText id="alert-dialog-description">
          
            <TextField  ref={amountRef} id="outlined-basic" label="Amount" required type='number' variant="outlined" />
            <br/>
            <TextField ref={detailsRef} id="outlined-basic" label="Details" required type='text' variant="outlined" />
        
          </DialogContentText>
        </DialogContent>
        <DialogActions>
          <Button type='submit' onClick={props.handleModalStatus.bind(this,'income')} color="primary">
            Save
          </Button>
          <Button onClick={props.handleModalStatus.bind(this,'income')} color="primary">
            Cancle
          </Button>
        </DialogActions>
        </form>
      </Dialog>
    </div>
  );
};

export default AddIncome;
